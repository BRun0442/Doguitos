function scrollUp()
{
  // document.body.scrollTop = 0;
  // document.documentElement.scrollTop = 0;

  //Subir a página quando clicar no botão         
  window.scrollTo({top: 0, behavior: 'smooth'});
}